Save your name in a variable inside script tag

Display name from the variable on the page

Calculate the cost of Myntra Bag and keep it in a variable

Show it to console

Keep GST percentage as constant

Use eval method from math to convert string calculation into result
